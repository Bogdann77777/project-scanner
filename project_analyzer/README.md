# Project Analyzer

Инструмент для анализа кодовых проектов с визуализацией зависимостей и автоматической генерацией описаний через LLM.

## Возможности

- ✅ Парсинг Python/JS/TS проектов
- ✅ Граф зависимостей функций
- ✅ Детектирование проблем (мертвый код, обрывы логики, заглушки)
- ✅ LLM-описания функций (что делает и КАК)
- ✅ Интерактивная визуализация
- ✅ Смена LLM модели в настройках

## Установка

```bash
pip install -r requirements.txt
```

## Настройка

1. Создайте файл `.env` в корне проекта
2. Добавьте ваш OpenRouter API ключ:

```
OPENROUTER_API_KEY=sk-or-v1-...
```

## Запуск

### 🚀 Быстрый запуск (Windows):

**Для первого запуска:**
```bash
start.bat
```
→ Проверяет зависимости, создает .env, показывает прогресс

**Для последующих запусков:**
```bash
RUN.bat
```
→ Мгновенный запуск без проверок

**Остановка сервера:**
```bash
stop.bat
```

📖 **Подробная инструкция:** См. файл `HOW_TO_RUN.txt`

---

### 💻 Ручной запуск:

```bash
python ui/app.py
```

Откройте браузер: `http://localhost:8000`

## Использование

1. Перетащите папку проекта в Upload Zone
2. Нажмите "Start Analysis"
3. Дождитесь завершения (2-3 минуты)
4. Изучайте граф зависимостей, проблемы, описания функций

## Структура проекта

```
project_analyzer/
├── config.py          # Конфигурация
├── parser.py          # AST парсинг кода
├── analyzer.py        # Анализ проблем
├── llm_describer.py   # Генерация описаний через LLM
├── visualizer.py      # Подготовка данных для UI
├── main.py            # Оркестратор
├── requirements.txt   # Python зависимости
├── .env              # Переменные окружения
├── README.md         # Документация
└── ui/
    ├── app.py        # Flask веб-сервер
    ├── templates/
    │   └── index.html
    └── static/
        ├── css/
        │   └── styles.css
        └── js/
            └── graph.js
```

## Технологии

- **Backend**: Python + Flask
- **Frontend**: HTML5 + CSS3 + JavaScript
- **Визуализация**: vis.js (граф)
- **LLM API**: OpenRouter
- **Анализ**: NetworkX (граф зависимостей)

## Как это работает

### 1. Парсинг (parser.py)
- Сканирует проект рекурсивно
- Строит AST для Python файлов
- Извлекает функции, классы, импорты
- Определяет вызовы между функциями

### 2. Анализ (analyzer.py)
- Строит граф вызовов через NetworkX
- Находит мертвый код (функции без входящих вызовов)
- Находит обрывы (вызовы несуществующих функций)
- Находит заглушки (pass, TODO, NotImplementedError)
- Проверяет соответствие return type и return statement

### 3. LLM Описания (llm_describer.py)
- Отправляет батчи функций в OpenRouter API
- Генерирует детальные описания (что и КАК делает)
- Обрабатывает ошибки с fallback описаниями

### 4. Визуализация (visualizer.py)
- Готовит данные для vis.js графа
- Строит дерево файлов
- Группирует проблемы по типам
- Считает статистику

### 5. UI (Flask + vis.js)
- Drag & Drop загрузка проектов
- Real-time прогресс анализа
- Интерактивный граф зависимостей
- Панели: файлы, граф, проблемы
- Модальные окна с деталями функций

## API Endpoints

- `GET /` - Главная страница
- `POST /analyze` - Запуск анализа
- `GET /progress/<project_id>` - Прогресс анализа
- `GET /results/<project_id>` - Результаты анализа
- `GET /config` - Текущая конфигурация
- `POST /config/model` - Обновление LLM модели

## Конфигурация

Все настройки в `config.py`:

- `OPENROUTER_API_KEY` - API ключ
- `LLM_MODEL` - Модель LLM (по умолчанию Claude 3.5 Haiku)
- `AVAILABLE_MODELS` - Доступные модели
- `MAX_FUNCTIONS_PER_BATCH` - Батч размер для LLM (10)
- `SUPPORTED_EXTENSIONS` - Поддерживаемые форматы (.py, .js, .ts, .jsx, .tsx)
- `IGNORE_DIRS` - Игнорируемые папки (node_modules, __pycache__, .git, venv)
- `UI_PORT` - Порт сервера (8000)
- `LLM_TIMEOUT` - Таймаут LLM запроса (60 сек)
- `MAX_FILE_SIZE` - Максимальный размер файла (1MB)

## Детектируемые проблемы

| Тип | Severity | Описание |
|-----|----------|----------|
| dead_code | warning | Функция никогда не вызывается |
| broken_call | error | Вызов несуществующей функции |
| placeholder | warning | Функция содержит только pass |
| placeholder | info | Функция содержит TODO/FIXME |
| placeholder | warning | Функция raises NotImplementedError |
| missing_return | error | Функция с return type без return |
| unused_import | info | Импорт не используется |

## Лицензия

MIT

## Автор

Generated with Claude Code
